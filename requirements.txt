# ── Core web framework ───────────────────────────────────────────────────────
flask
Werkzeug

# ── Environment & config ─────────────────────────────────────────────────────
python-dotenv

# ── Image processing ─────────────────────────────────────────────────────────
Pillow
opencv-python
numpy

# ── System monitoring ────────────────────────────────────────────────────────
psutil

# ── Audio ────────────────────────────────────────────────────────────────────
mutagen
pydub

# ── ChatterBox TTS subprocess server ─────────────────────────────────────────
# chatterbox-tts pulls in torch, torchaudio, transformers, etc.
# Install CUDA-enabled torch FIRST (see README), then:
#   pip install chatterbox-tts
chatterbox-tts
# FastAPI + uvicorn power the local TTS subprocess API server
fastapi
uvicorn[standard]
TorchCodec

# ── Pinterest scraper ────────────────────────────────────────────────────────
# NOTE: pinterest-dl[image] pins pillow==10.4.0 which fails on Python 3.14.
# We install pinterest-dl without the [image] extra (see setup instructions
# below) and list its runtime deps here manually instead.
#
# To set up Pinterest scraping after installing requirements.txt:
#   pip install pinterest-dl --no-deps
#   playwright install firefox
#
playwright
selenium
requests
Deprecated
m3u8
tqdm
# Perceptual hashing for duplicate image detection
imagehash
# YOLO object detection for content filtering (person / face detection)
# yolo11n.pt is auto-downloaded on first use (~5 MB, cached in project root)
ultralytics
